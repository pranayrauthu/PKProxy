import React from "react";

// TODO: Need to add snack bar notification..
const Notifier = (props) => {
    return (
        <div>
            I am an Notifier
        </div>
    )
}

export default Notifier;